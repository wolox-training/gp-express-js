const chai = require('chai'),
  User = require('../app/models').User,
  dictum = require('dictum.js'),
  server = require('./../app'),
  should = chai.should();

describe('album', () => {
  describe('/album GET', () => {});
});

chai;
